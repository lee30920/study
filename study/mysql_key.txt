1. Key(Index)
DB에서 검색속도를 높이기 위해 사용하는 기술

2. Primary Key
기본키.
테이블내 데이터의 고유번호이다.
값이 중복이 안되고
NULL값은 절대 불가능하다.

3.Unique Key
고유키.
이름에 맞게 값을 중복으로 넣을 수 없으며
대신에 NULL값을 넣을 수 있다.
테이블당 여러개를 가질 수 있다.

4.Foreign Key
테이블 내의 열 중 다른 테이블의 기본키를 참조하는 열